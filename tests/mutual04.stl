
type Either a b = < Left : a, Right : b >

mutual (a : Type)
  | Even = < EZero : a, ESucc : Odd >
  | Odd  = < OZero : a, OSucc : Even >
  | Foo  = forall (a : Nat). Nat a -> Either Even Odd

#eval Foo
